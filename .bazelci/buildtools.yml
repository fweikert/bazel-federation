---
platforms:
  ubuntu1604:
    test_targets:
    - "@com_github_bazelbuild_buildtools//:tests"
  ubuntu1804:
    test_targets:
    - "@com_github_bazelbuild_buildtools//:tests"
  macos:
    test_targets:
    - "@com_github_bazelbuild_buildtools//:tests"
  windows:
    test_flags:
    - "--enable_runfiles"
    test_targets:
    - "--"
    - "@com_github_bazelbuild_buildtools//:tests"